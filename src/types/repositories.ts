export type TRepositories = {
    id: number
    name: string
    owner: string
    topic: Array<string>
    created_at: string
    description?: string
    website?: string
    github: string
    preview?: string
}
